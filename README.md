# NFTS+share-permissions-lab
Configured shared folders in Windows Server and implented access control using both share and NFTS permissions. Used AD security groups to manager access and validated permissions from a domin-joined client machine.
